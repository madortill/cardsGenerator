תרגול + מבחן (יש כפתורים בהערות, הפונקציונליות לא מוכנה)

הוראות הפעלה, אולי עם ספרייה שדומה ל intro.js
יש תמונות בתיק מסכים

שגיאות בהטמעת סרטון יוטיוב בעזרת youtube api
embed youtube videos with youtube player api and not in html

Text cards will be rich text format with options for unordered list, ordered list, bold, title (using cards freeForm option)

use history API to return from InputScreen to MainScreen when back button in chrome or android is pressed